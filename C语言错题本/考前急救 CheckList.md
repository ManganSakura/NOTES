
1. **char 是有符号的**：`0x80` 以上的 char 打印出来是负数。
    
2. **逗号表达式看最后**：但前面的 `++` 都要算。
    
3. **Swap 函数传地址**：中间变量用 `int` 别用 `int*`。
    
4. **指针指结构体**：用 `->`，不要用 `.`。
    
5. **循环右移 N 次**：如果是 N 个元素，结果就是**原封不动**。。